# https://wandb.ai

_target_: iid.utils.CustomTensorBoardLogger
name: "intrinsic_image_diffusion" # name of the run (normally generated by wandb)
save_dir: "${paths.output_dir}"
version: "0"
log_graph: true
prefix: "" # a string to put at the beginning of metric keys